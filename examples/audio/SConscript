Import('env')
build = env.Clone(BUILD = '.build')
build.VariantDir('$BUILD', '.', duplicate = 0)
example = build.Program('#/bin/examples/audio', build.Glob('$BUILD/*.cpp'))
build.Clean(example, build.Dir('$BUILD'))
Return('example')
